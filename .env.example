# === Authentication (choose one) ===

# Option 1: Google AI Studio API key (recommended for local dev)
# Get one at: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_api_key_here

# Option 2: Vertex AI mode (for Cloud Run / production)
# PROJECT_ID=your-project-id
# LOCATION=us-central1

# === Settings ===

# Model override (auto-detected based on auth mode if not set)
# MODEL=gemini-2.5-flash-preview-native-audio-dialog

# Development Mode (skips reCAPTCHA + rate limiting)
DEV_MODE=true

# Session time limit in seconds
SESSION_TIME_LIMIT=600
